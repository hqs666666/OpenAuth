server:
  port: 7000

spring:
  datasource:
    druid:
      url: jdbc:mysql://47.100.229.56:3306/oauthdb?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=CTT
      username: root
      password: Mk;c6[cB424n
      initial-size: 1
      min-idle: 1
      max-active: 20
      test-on-borrow: true
      driver-class-name: com.mysql.cj.jdbc.Driver
      test-while-idle: true

  thymeleaf:
    mode: LEGACYHTML5
    prefix: classpath:/templates/
    suffix: .html
    template-resolver-order: 0
    cache: false

  redis:
    database: 7
    host: hqs.pub
    port: 6380
    password: redis123456aB

custom:
  access-token:
    expires-hour: 2
  refresh-token:
    expires-day: 7

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
